# JARVIS II Mobile Thin Client — Environment Variables
# This file is gitignored. Edit freely — it never leaves your device.

export JARVIS_MODE=CONTROLLER

# HiVEMiND Tailscale network (all three are equivalent — IP is most reliable)
export HIVEMIND_IP=100.86.132.90              # Tailscale IPv4
# export HIVEMIND_IP=hivemind.tailcbd4e0.ts.net  # Tailscale hostname (alternative)
# export HIVEMIND_IP=fd7a:115c:a1e0::c301:8499   # Tailscale IPv6 (alternative)

export JARVIS_LOCAL_PORT=8765          # Local gateway port on the phone
export JARVIS_LOG_LEVEL=INFO           # DEBUG | INFO | WARNING

# Optional per-service port overrides (uncomment to override defaults)
# export SDWEBUI_PORT=7860
# export COMFYUI_PORT=8188
# export FASTAPI_PORT=8000
# export OLLAMA_PORT=11434
# export TTS_PORT=5002
# export WHISPER_PORT=9000
